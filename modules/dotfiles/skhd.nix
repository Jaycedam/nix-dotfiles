{
  enable = true;
  skhdConfig = ''
    cmd + alt - t : open -a "Alacritty"
    cmd + alt - b : open -a "Safari"
    cmd + alt - m : open -a "Spotube"
    cmd + alt - n : open -a "Notes"
    cmd + alt - e : open -a "Mail"
  '';
}
